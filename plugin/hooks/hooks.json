{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/warn-lockit-pull.sh",
            "statusMessage": "Checking for unsafe lockit pull usage..."
          }
        ]
      }
    ]
  }
}
